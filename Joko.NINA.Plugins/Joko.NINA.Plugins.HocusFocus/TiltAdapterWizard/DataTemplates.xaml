<ResourceDictionary
    x:Class="NINA.Joko.Plugins.HocusFocus.TiltAdapterWizard.DataTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ninactrl="clr-namespace:NINA.CustomControlLibrary;assembly=NINA.CustomControlLibrary"
    xmlns:rules="clr-namespace:NINA.Core.Utility.ValidationRules;assembly=NINA.Core"
    xmlns:s="clr-namespace:System;assembly=mscorlib"
    xmlns:interfaces="clr-namespace:NINA.Joko.Plugins.HocusFocus.Interfaces">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Resources/OptionsDataTemplates.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--
        Sensor frame outline (10px thick border, EvenOdd) plus three diamond-shaped screw
        indicators at the 0° / 120° / 240° positions of a standard 3-screw tilt adapter.
    -->
    <GeometryGroup x:Key="TiltAdapterWizardSVG">
        <PathGeometry Figures="M 55,65 L 145,65 L 145,115 L 55,115 Z M 65,75 L 65,105 L 135,105 L 135,75 Z" FillRule="EvenOdd" />
        <PathGeometry Figures="M 100,15 L 120,35 L 100,55 L 80,35 Z" FillRule="Nonzero" />
        <PathGeometry Figures="M 155,125 L 175,145 L 155,165 L 135,145 Z" FillRule="Nonzero" />
        <PathGeometry Figures="M 45,125 L 65,145 L 45,165 L 25,145 Z" FillRule="Nonzero" />
    </GeometryGroup>

    <!--
        Diagram canvas shared between Panel A (saved calibration) and Panel C (complete step).
        Layout: 200×200 canvas, center (100,100).
          • Sensor rectangle: 90×60 (3:2), centered → (55,70)-(145,130)
          • Sensor-axis crosshair: short dashes (StrokeDashArray="4,4"), opacity 0.5
          • Screw connection polygon: long dashes (StrokeDashArray="10,4"), opacity 0.75
          • Screw circles: radius 75 from center, 24×24 circles
    -->
    <DataTemplate x:Key="HF_TiltScrewDiagram">
        <Canvas Width="200" Height="200">
            <!--  Sensor rectangle — 3:2 aspect ratio, 90×60  -->
            <Rectangle
                Canvas.Left="55"
                Canvas.Top="70"
                Width="90"
                Height="60"
                Fill="Transparent"
                Stroke="{StaticResource PrimaryBrush}"
                StrokeThickness="1.5" />

            <!--  Sensor-axis crosshairs — short-dash dotted  -->
            <Line
                Opacity="0.5"
                Stroke="{StaticResource PrimaryBrush}"
                StrokeDashArray="4,4"
                StrokeThickness="0.75"
                X1="0"
                X2="200"
                Y1="100"
                Y2="100" />
            <Line
                Opacity="0.5"
                Stroke="{StaticResource PrimaryBrush}"
                StrokeDashArray="4,4"
                StrokeThickness="0.75"
                X1="100"
                X2="100"
                Y1="0"
                Y2="200" />

            <!--  Screw connection polygon — long-dash lines  -->
            <ItemsControl
                Canvas.Left="0"
                Canvas.Top="0"
                Width="200"
                Height="200"
                ItemsSource="{Binding ScrewConnectionLines}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Line
                            Opacity="0.75"
                            Stroke="{StaticResource PrimaryBrush}"
                            StrokeDashArray="10,4"
                            StrokeThickness="1"
                            X1="{Binding X1}"
                            X2="{Binding X2}"
                            Y1="{Binding Y1}"
                            Y2="{Binding Y2}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!--  Screw circles (on top of connection lines)  -->
            <ItemsControl
                Canvas.Left="0"
                Canvas.Top="0"
                Width="200"
                Height="200"
                ItemsSource="{Binding ScrewDiagramItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="24" Height="24">
                            <Ellipse
                                Fill="{StaticResource ButtonBackgroundBrush}"
                                Stroke="{StaticResource PrimaryBrush}"
                                StrokeThickness="1.5" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontWeight="Bold"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="{Binding Number}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </DataTemplate>

    <!--
        Calibration info rows shared between Panel A and Panel C.
        Matches inspector UniformGrid Columns="2" label+value pattern.
        Screw 4 row collapses for 3-screw setups.
        Curvature row shows the configured/measured direction of the curvature response.
    -->
    <DataTemplate x:Key="HF_TiltScrewCalibrationInfo">
        <StackPanel>
            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                <TextBlock HorizontalAlignment="Center" Text="Screw 1" />
                <TextBlock HorizontalAlignment="Center" Text="{Binding TiltAdapterOptions.Screw1AngleDegrees, StringFormat={}{0:F1}°}" />
            </UniformGrid>
            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                <TextBlock HorizontalAlignment="Center" Text="Screw 2" />
                <TextBlock HorizontalAlignment="Center" Text="{Binding TiltAdapterOptions.Screw2AngleDegrees, StringFormat={}{0:F1}°}" />
            </UniformGrid>
            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                <TextBlock HorizontalAlignment="Center" Text="Screw 3" />
                <TextBlock HorizontalAlignment="Center" Text="{Binding TiltAdapterOptions.Screw3AngleDegrees, StringFormat={}{0:F1}°}" />
            </UniformGrid>
            <!--  Screw 4 row — collapses when ScrewCount == 3  -->
            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                <UniformGrid.Style>
                    <Style TargetType="UniformGrid">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TiltAdapterOptions.ScrewCount}" Value="3">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </UniformGrid.Style>
                <TextBlock HorizontalAlignment="Center" Text="Screw 4" />
                <TextBlock HorizontalAlignment="Center" Text="{Binding TiltAdapterOptions.Screw4AngleDegrees, StringFormat={}{0:F1}°}" />
            </UniformGrid>
            <!--  Curvature row — the arrow shows how the curvature effect responds to clockwise screw turns  -->
            <UniformGrid
                Margin="0,2"
                VerticalAlignment="Center"
                Columns="2"
                ToolTip="The direction of the curvature response to clockwise screw turns (+ steps for stepper motors). Set by the adapter direction in the Measurement settings, or measured by a 6-step calibration run.">
                <UniformGrid.Style>
                    <Style TargetType="UniformGrid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasCurvatureCalibration}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </UniformGrid.Style>
                <TextBlock HorizontalAlignment="Center" Text="Screws CW" />
                <TextBlock HorizontalAlignment="Center">
                    <Run Text="Curvature " /><Run Text="{Binding CurvatureSignDescription, Mode=OneWay}" />
                </TextBlock>
            </UniformGrid>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="NINA.Joko.Plugins.HocusFocus.TiltAdapterWizard.TiltAdapterWizardVM_Dockable">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="5" HorizontalAlignment="Left" MinWidth="340">

                <!-- Panel A: Settings + launch (visible when !IsWizardRunning) -->
                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsWizardRunning}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock
                        Margin="0,0,0,5"
                        FontWeight="Bold"
                        Text="Settings" />

                    <!--  Device preset: fills and locks the hardware fields below  -->
                    <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Device"
                            ToolTip="Select a known tilt adapter to auto-fill and lock its hardware values, or choose Manual to enter them yourself." />
                        <ComboBox MinWidth="120" ItemsSource="{Binding DeviceNames}" SelectedItem="{Binding SelectedDevice}" />
                    </UniformGrid>

                    <!--  Adapter hardware: drives precise screw-turn amounts in the Aberration Inspector.
                          Disabled when a non-Manual device preset is selected.  -->
                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Screws"
                            ToolTip="Number of adjustment screws on the tilt adapter (3 = independent screws, 4 = coupled opposite pairs)." />
                        <ComboBox MinWidth="60" IsEnabled="{Binding IsManualDevice}" SelectedItem="{Binding TiltAdapterOptions.ScrewCount}">
                            <ComboBox.ItemsSource>
                                <x:Array Type="{x:Type s:Int32}">
                                    <s:Int32>3</s:Int32>
                                    <s:Int32>4</s:Int32>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>
                    </UniformGrid>

                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Adjustment type"
                            ToolTip="Whether the adapter is adjusted by turning screws or by stepper motors. Determines whether amounts are shown as turns or steps." />
                        <ComboBox MinWidth="120" IsEnabled="{Binding IsManualDevice}" SelectedValuePath="Tag" SelectedValue="{Binding AdjustmentType}">
                            <ComboBoxItem Content="Screws" Tag="{x:Static interfaces:TiltAdjustmentType.Screws}" />
                            <ComboBoxItem Content="Stepper Motors" Tag="{x:Static interfaces:TiltAdjustmentType.StepperMotors}" />
                        </ComboBox>
                    </UniformGrid>

                    <!--  Thread pitch (screws only)  -->
                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <UniformGrid.Style>
                            <Style TargetType="UniformGrid">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStepperAdjustment}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </UniformGrid.Style>
                        <TextBlock VerticalAlignment="Center" Text="Thread pitch (µm/turn)"
                            ToolTip="Axial sensor travel per full screw turn, in microns. Converts the required focus correction into a number of turns." />
                        <TextBox MinWidth="60" IsEnabled="{Binding IsManualDevice}" Text="{Binding ThreadPitchMicronsValue, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <!--  Step size (steppers only)  -->
                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <UniformGrid.Style>
                            <Style TargetType="UniformGrid">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStepperAdjustment}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </UniformGrid.Style>
                        <TextBlock VerticalAlignment="Center" Text="Step size (µm/step)"
                            ToolTip="Axial sensor travel per stepper step, in microns. Converts the required focus correction into a number of steps." />
                        <TextBox MinWidth="60" IsEnabled="{Binding IsManualDevice}" Text="{Binding StepperStepSizeMicronsValue, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Screw radius (mm)"
                            ToolTip="Distance from the sensor center to each screw, in millimeters. Sets the lever arm used to convert tilt into screw adjustments." />
                        <TextBox MinWidth="60" IsEnabled="{Binding IsManualDevice}" Text="{Binding ScrewRadiusMillimetersValue, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <!--  Editable per-session fields (kept together, always editable)  -->
                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Measurements"
                            ToolTip="Number of autofocus runs to average per calibration step. Higher values reduce noise but take longer." />
                        <TextBox
                            MinWidth="60"
                            Text="{Binding TiltAdapterOptions.MeasurementAverageCount, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="{Binding CalibrationAmountLabel}"
                            ToolTip="How far you turn each screw (or move each stepper) during the per-screw calibration steps. Used to compute the measured thread pitch / step size." />
                        <TextBox MinWidth="60" Text="{Binding CalibrationAppliedAmount, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <!--  Shared inputs that feed the calculation — grouped below the per-session fields.
                          Editing these writes through to their source of truth (camera profile / Inspector options).  -->
                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Pixel size (µm)"
                            ToolTip="Camera pixel size in microns. Editing here updates your NINA camera profile — the same value used everywhere." />
                        <TextBox MinWidth="60" Text="{Binding PixelSizeMicronsValue, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <UniformGrid Margin="0,5" VerticalAlignment="Center" Columns="2">
                        <TextBlock VerticalAlignment="Center" Text="Focuser step size (µm)"
                            ToolTip="Microns of focus travel per focuser step. Editing here updates the Hocus Focus Aberration Inspector's 'Microns per focuser step' setting." />
                        <TextBox MinWidth="60" Text="{Binding FocuserStepSizeMicronsValue, UpdateSourceTrigger=LostFocus}" />
                    </UniformGrid>

                    <!--  Measurement cost & direction settings (shared Signal Amplification /
                          Center Focuser First live in InspectorOptions — same values as the
                          Aberration Inspector's controls)  -->
                    <TextBlock
                        Margin="0,10,0,5"
                        FontWeight="Bold"
                        Text="Measurement" />
                    <Grid Margin="0,2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Margin="0,2,5,2"
                            VerticalAlignment="Center"
                            Text="Signal Amplification"
                            ToolTip="Increases the resolution and signal of sensor-model / tilt calibration runs by capturing more, finer-spaced focuser points. Set to 1 to disable. Applies to live captures only." />
                        <ninactrl:UnitTextBox
                            Grid.Row="0"
                            Grid.Column="1"
                            MinWidth="40"
                            VerticalAlignment="Center"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            Foreground="{StaticResource PrimaryBrush}"
                            TextAlignment="Left"
                            Unit="x">
                            <Binding
                                Mode="TwoWay"
                                Path="Inspector.InspectorOptions.SignalAmplification"
                                UpdateSourceTrigger="LostFocus" />
                        </ninactrl:UnitTextBox>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="2"
                            Margin="8,2,0,2"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Opacity="0.7"
                            Text="{Binding WizardSweepSummary}"
                            TextWrapping="Wrap" />

                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Margin="0,2,5,2"
                            VerticalAlignment="Center"
                            Text="Center Focuser First"
                            ToolTip="When on, a quick standard AutoFocus is run before each live sweep to center the focuser at best focus. Off by default. No effect when replaying saved frames." />
                        <CheckBox
                            Grid.Row="1"
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            IsChecked="{Binding Inspector.InspectorOptions.CenterFocuserBeforeRun}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="2"
                            Margin="8,2,0,2"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Opacity="0.7"
                            Text="Adds one standard autofocus before every measurement sweep to re-center focus. Turn on if focus drifts between steps (e.g., temperature) or your focuser starts far from focus."
                            TextWrapping="Wrap" />

                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Margin="0,2,5,2"
                            VerticalAlignment="Center"
                            Text="{Binding CwDirectionLabel}"
                            ToolTip="Whether tightening moves the adapter toward the camera or the objective depends on its design (push vs pull screws). Sets the direction of backfocus/curvature guidance." />
                        <ComboBox
                            Grid.Row="2"
                            Grid.Column="1"
                            MinWidth="180"
                            VerticalAlignment="Center"
                            SelectedValuePath="Tag"
                            SelectedValue="{Binding CwMovesAdapterTowardObjective}">
                            <ComboBox.Style>
                                <Style BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="ComboBox">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TiltAdapterOptions.MeasureCurvatureDuringCalibration}" Value="True">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                            <ComboBoxItem Content="Toward the camera — outward">
                                <ComboBoxItem.Tag>
                                    <s:Boolean>False</s:Boolean>
                                </ComboBoxItem.Tag>
                            </ComboBoxItem>
                            <ComboBoxItem Content="Toward the objective — inward">
                                <ComboBoxItem.Tag>
                                    <s:Boolean>True</s:Boolean>
                                </ComboBoxItem.Tag>
                            </ComboBoxItem>
                        </ComboBox>
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="2"
                            Margin="8,2,0,2"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Opacity="0.7"
                            Text="{Binding CurvatureSignProvenance}"
                            TextWrapping="Wrap" />

                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Margin="0,2,5,2"
                            VerticalAlignment="Center"
                            Text="Measure direction"
                            ToolTip="Include the two curvature-direction steps in the calibration to determine the adapter direction automatically." />
                        <CheckBox
                            Grid.Row="3"
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            IsChecked="{Binding TiltAdapterOptions.MeasureCurvatureDuringCalibration}" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="2"
                            Margin="8,2,0,2"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Opacity="0.7"
                            Text="Adds 2 extra measurement steps (6 instead of 4 — about 50% longer) to determine the direction automatically. Turn on if you don't know how your adapter behaves, or to verify the setting above."
                            TextWrapping="Wrap" />
                    </Grid>

                    <!--  Calibrate + Replay side by side  -->
                    <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                        <Button Command="{Binding StartCommand}">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Calibrate" />
                        </Button>
                        <Button
                            Margin="6,0,0,0"
                            Command="{Binding ReplayCommand}"
                            ToolTip="Replay a saved calibration run from a folder containing metadata.json. Choose how in the prompt: your current settings, the run's capture-time settings held in memory (profile untouched), or after updating your profile to the run's capture-time settings.">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Replay" />
                        </Button>
                    </StackPanel>

                    <!--  Save calibration for replay (opt-in per run; folder persisted for reuse, toggle resets each run)  -->
                    <StackPanel
                        Margin="0,8,0,0"
                        Orientation="Horizontal"
                        ToolTip="Save each calibration step's AutoFocus sweep into a per-run folder with a metadata.json so the run can be replayed later. Off by default; enable before each run.">
                        <CheckBox VerticalAlignment="Center" IsChecked="{Binding SaveAFRuns}" Checked="SaveAFRuns_Checked" />
                        <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="Save Calibration for Replay" />
                    </StackPanel>
                    <Grid Margin="0,4,0,0" HorizontalAlignment="Stretch">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SaveAFRuns}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="{Binding SaveAFRunsPath, UpdateSourceTrigger=LostFocus}"
                            ToolTip="Folder where calibration runs are saved. A TiltCalibration_&lt;timestamp&gt; subfolder is created per run." />
                        <Button Grid.Column="1" Margin="6,0,0,0" Command="{Binding BrowseSaveFolderCommand}">
                            <TextBlock
                                Margin="10,3,10,3"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Browse…" />
                        </Button>
                    </Grid>

                    <TextBlock Margin="0,10,0,0" Text="No calibration saved.">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalibrationValid}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!--  Saved calibration sub-panel  -->
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCalibrationValid}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <TextBlock
                            Margin="0,10,0,0"
                            FontStyle="Italic"
                            Opacity="0.7"
                            Text="Manually entered calibration (not measured by the wizard)."
                            TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TiltAdapterOptions.CalibrationIsManual}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Grid Margin="0,10,0,0" HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentControl
                                Grid.Column="0"
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                ContentTemplate="{StaticResource HF_TiltScrewCalibrationInfo}"
                                Content="{Binding}" />
                            <ContentControl
                                Grid.Column="1"
                                ContentTemplate="{StaticResource HF_TiltScrewDiagram}"
                                Content="{Binding}" />
                        </Grid>
                    </StackPanel>

                    <!--  Manual calibration entry: writes the same persisted state a wizard run
                          does (screw angles + calibrated flags), tagged as manually entered.  -->
                    <Expander Margin="0,10,0,0" Header="Manual Calibration Entry">
                        <StackPanel Margin="0,5,0,0">
                            <TextBlock
                                Margin="0,0,0,5"
                                FontStyle="Italic"
                                Opacity="0.7"
                                Text="If you already know where screw 1 sits, enter it here and skip the wizard. Angles and numbering direction are in image space — mirrors or diagonals in the optical train can flip them relative to the physical adapter. If guidance moves tilt the wrong way, flip the numbering direction. Set the adapter direction in the Measurement settings above if you know it. If you change the adapter direction setting later, click Apply again."
                                TextWrapping="Wrap" />
                            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                                <TextBlock VerticalAlignment="Center" Text="Screw 1 angle (°)"
                                    ToolTip="Position angle of screw 1 in the image: 0° = straight up (12 o'clock), increasing clockwise. Remaining screws are placed at equal spacing (120° for 3 screws, 90° for 4)." />
                                <!--  Validation keeps the box in an error state on unparseable/out-of-range text: without it a
                                      failed LostFocus conversion silently leaves the last valid angle in the VM while the box
                                      shows the garbage text, and Apply would persist the stale angle.  -->
                                <TextBox x:Name="ManualScrew1AngleBox" MinWidth="60">
                                    <TextBox.Text>
                                        <Binding Path="ManualScrew1AngleDegrees" UpdateSourceTrigger="LostFocus">
                                            <Binding.ValidationRules>
                                                <rules:DoubleRangeRule>
                                                    <rules:DoubleRangeRule.ValidRange>
                                                        <rules:DoubleRangeChecker Maximum="360" Minimum="0" />
                                                    </rules:DoubleRangeRule.ValidRange>
                                                </rules:DoubleRangeRule>
                                            </Binding.ValidationRules>
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>
                            </UniformGrid>
                            <UniformGrid Margin="0,2" VerticalAlignment="Center" Columns="2">
                                <TextBlock VerticalAlignment="Center" Text="Numbering direction"
                                    ToolTip="Whether screws 2 and up proceed clockwise or counter-clockwise from screw 1, as seen in the image." />
                                <ComboBox MinWidth="120" SelectedValuePath="Tag" SelectedValue="{Binding ManualNumberingClockwise}">
                                    <ComboBoxItem Content="Clockwise">
                                        <ComboBoxItem.Tag>
                                            <s:Boolean>True</s:Boolean>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                    <ComboBoxItem Content="Counter-clockwise">
                                        <ComboBoxItem.Tag>
                                            <s:Boolean>False</s:Boolean>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                </ComboBox>
                            </UniformGrid>
                            <Button
                                Margin="0,8,0,0"
                                HorizontalAlignment="Left"
                                Command="{Binding ApplyManualCalibrationCommand}">
                                <Button.Style>
                                    <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=ManualScrew1AngleBox, Path=(Validation.HasError)}" Value="True">
                                                <Setter Property="IsEnabled" Value="False" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <TextBlock
                                    Margin="10,5,10,5"
                                    Foreground="{StaticResource ButtonForegroundBrush}"
                                    Text="Apply" />
                            </Button>
                        </StackPanel>
                    </Expander>
                </StackPanel>

                <!-- Panel B: Active wizard steps (visible when IsWizardRunning && !IsComplete) -->
                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsWizardRunning}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock
                        Margin="0,0,0,10"
                        Text="{Binding StepInstructions}"
                        TextWrapping="Wrap" />

                    <!--  Run Measurement / Use Saved AF: visible on measurement steps while not already measuring  -->
                    <StackPanel
                        Margin="0,0,0,4"
                        HorizontalAlignment="Left"
                        Orientation="Horizontal">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOnMeasurementStep}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsMeasuring}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <Button
                            Margin="0,0,6,0"
                            Command="{Binding RunMeasurementCommand}">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Run Measurement" />
                        </Button>
                        <Button Command="{Binding UseSavedAFCommand}">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Use Saved AF" />
                        </Button>
                    </StackPanel>

                    <!--  Device connection warning: shown when Run Measurement is visible but devices are not connected  -->
                    <TextBlock
                        Margin="0,0,0,4"
                        Foreground="{StaticResource NotificationErrorBrush}"
                        Text="{Binding ConnectionWarningText}"
                        TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsOnMeasurementStep}" Value="True" />
                                            <Condition Binding="{Binding IsMeasuring}" Value="False" />
                                            <Condition Binding="{Binding AreDevicesConnected}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!--  Measurement progress area: visible while measuring  -->
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMeasuring}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock
                            Margin="0,0,0,4"
                            FontStyle="Italic"
                            Text="{Binding StatusText}"
                            TextWrapping="Wrap" />
                        <Button
                            HorizontalAlignment="Left"
                            Command="{Binding CancelCommand}">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Cancel" />
                        </Button>
                    </StackPanel>

                    <!--  Abort Wizard: hidden while a measurement is in progress  -->
                    <Button
                        Margin="0,10,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding RestartCommand}">
                        <Button.Style>
                            <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMeasuring}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock
                            Margin="10,5,10,5"
                            Foreground="{StaticResource ButtonForegroundBrush}"
                            Text="Abort Wizard" />
                    </Button>

                    <!--  Post-measurement status (shown when not measuring)  -->
                    <TextBlock
                        Margin="0,4,0,0"
                        Text="{Binding StatusText}"
                        TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMeasuring}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!--  AutoFocus / sensor-model failure choice: run AF again, or how to return to baseline  -->
                    <Border
                        Margin="0,6,0,0"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMeasurementFailureChoice}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,6"
                                Foreground="{StaticResource NotificationErrorBrush}"
                                Text="{Binding MeasurementFailureText}"
                                TextWrapping="Wrap" />
                            <Button
                                Margin="0,0,0,6"
                                HorizontalAlignment="Left"
                                Command="{Binding RetryMeasurementCommand}">
                                <TextBlock
                                    Margin="10,5,10,5"
                                    Foreground="{StaticResource ButtonForegroundBrush}"
                                    Text="Run AutoFocus again" />
                            </Button>
                            <TextBlock
                                Margin="0,2,0,0"
                                FontWeight="Bold"
                                Text="To return to baseline:" />
                            <TextBlock
                                Margin="0,2,0,0"
                                Text="{Binding BaselineRecoveryInstructions}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <!--  Live inspector view: shown only during an active measurement  -->
                    <ContentControl
                        Margin="0,8,0,0"
                        Content="{Binding Inspector}"
                        ContentTemplate="{StaticResource HF_InspectorLiveView}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMeasuring}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>

                    <!--  Consistency warning (shown when tilt measurements across runs diverge)  -->
                    <Border
                        Margin="0,6,0,0"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMeasurementConsistencyWarning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock
                            Foreground="{StaticResource NotificationErrorBrush}"
                            Text="{Binding MeasurementConsistencyWarningText}"
                            TextWrapping="Wrap" />
                    </Border>

                    <!--  Measurement results: single run (no Run column)  -->
                    <StackPanel Margin="0,8,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding HasMeasurementResults}" Value="True" />
                                            <Condition Binding="{Binding ShowRunColumn}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <UniformGrid Margin="0,0,0,2" Columns="3">
                            <TextBlock FontWeight="DemiBold" Text="Step" />
                            <TextBlock FontWeight="DemiBold" Text="Rotation Angle" />
                            <TextBlock FontWeight="DemiBold" Text="Tilt Angle" />
                        </UniformGrid>
                        <ItemsControl ItemsSource="{Binding StepMeasurementSummary}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <UniformGrid Columns="3">
                                        <TextBlock Text="{Binding StepDescription}" />
                                        <TextBlock Text="{Binding Direction, StringFormat={}{0:F1}°}" />
                                        <TextBlock Text="{Binding TiltAngleDisplay}" />
                                    </UniformGrid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!--  Measurement results: multiple runs (includes Run column, avg row bold)  -->
                    <StackPanel Margin="0,8,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding HasMeasurementResults}" Value="True" />
                                            <Condition Binding="{Binding ShowRunColumn}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <UniformGrid Margin="0,0,0,2" Columns="4">
                            <TextBlock FontWeight="DemiBold" Text="Step" />
                            <TextBlock FontWeight="DemiBold" Text="Run" />
                            <TextBlock FontWeight="DemiBold" Text="Rotation Angle" />
                            <TextBlock FontWeight="DemiBold" Text="Tilt Angle" />
                        </UniformGrid>
                        <ItemsControl ItemsSource="{Binding StepMeasurementSummary}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="TextElement.FontWeight" Value="Normal" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAverage}" Value="True">
                                                        <Setter Property="TextElement.FontWeight" Value="Bold" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <UniformGrid Columns="4">
                                            <TextBlock Text="{Binding StepDescription}" />
                                            <TextBlock Text="{Binding Label}" />
                                            <TextBlock Text="{Binding Direction, StringFormat={}{0:F1}°}" />
                                            <TextBlock Text="{Binding TiltAngleDisplay}" />
                                        </UniformGrid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>

                <!-- Panel C: Complete step (visible when IsComplete) -->
                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock
                        Margin="0,0,0,5"
                        FontWeight="Bold"
                        Text="Calibration Complete!" />

                    <TextBlock
                        Margin="0,0,0,5"
                        Text="{Binding StepInstructions}"
                        TextWrapping="Wrap" />

                    <Border
                        Margin="0,5"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasWarning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding WarningText}" TextWrapping="Wrap" />
                    </Border>

                    <!--  Consistency warning raised by the final step's repeats: it survives the advance to
                          Complete (the step panel that raised it is already hidden), so it must render here too  -->
                    <Border
                        Margin="0,5"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMeasurementConsistencyWarning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock
                            Foreground="{StaticResource NotificationErrorBrush}"
                            Text="{Binding MeasurementConsistencyWarningText}"
                            TextWrapping="Wrap" />
                    </Border>

                    <!--  Re-baseline drift warning (an undo between moves left residual tilt)  -->
                    <Border
                        Margin="0,5"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasRebaselineDriftWarning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock
                            Foreground="{StaticResource NotificationErrorBrush}"
                            Text="{Binding RebaselineDriftWarningText}"
                            TextWrapping="Wrap" />
                    </Border>

                    <!--  Low calibration confidence: measurement noise rivals the screw-move signal (do not apply)  -->
                    <Border
                        Margin="0,5"
                        Padding="5"
                        BorderBrush="{StaticResource NotificationErrorBrush}"
                        BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasConfidenceWarning}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock
                            Foreground="{StaticResource NotificationErrorBrush}"
                            Text="{Binding ConfidenceWarningText}"
                            TextWrapping="Wrap" />
                    </Border>

                    <Grid Margin="0,5,0,5" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ContentControl
                            Grid.Column="0"
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            ContentTemplate="{StaticResource HF_TiltScrewCalibrationInfo}"
                            Content="{Binding}" />
                        <ContentControl
                            Grid.Column="1"
                            ContentTemplate="{StaticResource HF_TiltScrewDiagram}"
                            Content="{Binding}" />
                    </Grid>

                    <!--  Measured thread pitch / step size vs the saved value  -->
                    <StackPanel Margin="0,10,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMeasuredHardware}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <TextBlock FontWeight="Bold" Text="Measured Adapter Hardware" />

                        <UniformGrid Margin="0,4,0,0" Columns="2">
                            <TextBlock Text="Measured" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding MeasuredHardwareDisplay}" />
                                <TextBlock Margin="8,0,0,0" Opacity="0.7" Text="{Binding HardwareDeltaDisplay}" />
                            </StackPanel>
                            <TextBlock Text="Saved" />
                            <TextBlock Text="{Binding SavedHardwareDisplay}" />
                        </UniformGrid>

                        <Button
                            Margin="0,8,0,2"
                            HorizontalAlignment="Left"
                            Command="{Binding UseMeasuredHardwareCommand}">
                            <TextBlock
                                Margin="10,5,10,5"
                                Foreground="{StaticResource ButtonForegroundBrush}"
                                Text="Use measured value" />
                        </Button>

                        <TextBlock Margin="0,6,0,2" FontStyle="Italic" Text="Inputs to this calculation:" />
                        <UniformGrid Columns="2">
                            <TextBlock Text="Pixel size" />
                            <TextBlock Text="{Binding CalibrationPixelSizeDisplay}" />
                            <TextBlock Text="Focuser step size" />
                            <TextBlock Text="{Binding CalibrationFocuserStepDisplay}" />
                            <TextBlock Text="Screw radius" />
                            <TextBlock Text="{Binding CalibrationScrewRadiusDisplay}" />
                            <TextBlock Text="Applied per screw" />
                            <TextBlock Text="{Binding CalibrationAppliedAmountDisplay}" />
                        </UniformGrid>
                    </StackPanel>

                    <TextBlock
                        Margin="0,10,0,0"
                        FontStyle="Italic"
                        Opacity="0.7"
                        Text="Set a screw radius (and thread pitch / step size) in Settings to measure adapter hardware."
                        TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMeasuredHardware}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button
                        Margin="0,8,0,2"
                        HorizontalAlignment="Left"
                        Command="{Binding RestartCommand}">
                        <TextBlock
                            Margin="10,5,10,5"
                            Foreground="{StaticResource ButtonForegroundBrush}"
                            Text="Done" />
                    </Button>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </DataTemplate>

</ResourceDictionary>
